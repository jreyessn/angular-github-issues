import { MapRepoPipe } from "../../../app/issues/pipes/map-repo.pipe";

describe('MapRepoPipe', () => {
  it('create an instance', () => {
    const pipe = new MapRepoPipe();
    expect(pipe).toBeTruthy();
  });
});
